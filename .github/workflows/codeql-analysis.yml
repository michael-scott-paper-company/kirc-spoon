name: HomebrewL

on:
  push:

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/checkout@v2
      with:
        ssh-key: ${{ secrets.GHAS_DEPLOY_KEY }}
        ref: composite-run-steps
        repository: michael-scott-paper-company/private-codeql-config
        path: .github/action/HomebrewL
    - uses: ./.github/action/HomebrewL
      with:
        language: cpp
        suite: security-and-quality
